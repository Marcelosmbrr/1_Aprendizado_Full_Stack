<?php

    //Ao invés de escrever "session_start()" em todas as sessões, podemos configurar um arquivo padrão para isso
    //Escrevemos require_once('Config_session') para que a função seja incluída sem estar implementada no própri em questão
    session_start();
    
    
