ativar.addEventListener('click', () => {
    //Se a classe "animar" não existir no item de id "modal", passará a existir
    //Se existir, deixará de existir
    
    modal.classList.toggle('animar'); 
  });