//Quando configurado como "Default" o import não precisa ser com o exato mesmo nome da estrutura importada
//Veja no arquivo "script.js"
export default function numeroAleatorio(){
    return Math.random();
}